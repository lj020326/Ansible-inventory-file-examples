- name: "Run trace var play"
  hosts: all
  gather_facts: false
  connection: local
  tasks:
    - fail:
        msg: 'Use --limit, Luke!'
      when: ansible_limit is not defined
    - debug:
        var: trace_var
