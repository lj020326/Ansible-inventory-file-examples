- name: "Run play"
  hosts: all
  gather_facts: false
  connection: local
  tasks:
    - name: Group hosts into cluster group in
      changed_when: no
      group_by:
        key: "cluster"
        parents: "override"
    - debug: var=test
